#pragma once

/*********************** Pin Definitions (Consistent with main project config.h) ***********************/

// Buttons
#define PIN_BOOT_KEY      ( 0)  // BOOT button, active LOW, requires pull-up
#define PIN_EC_BTN        (18)  // Rotary encoder button, active LOW, requires pull-up

/*********************** Pin Definitions ***********************/